name: tests

on:
  push:
    branches: [main]
    tags: ["v*"] # Push events to matching v*, i.e. v1.0, v20.15.10
  pull_request: {}
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.platform }} py${{ matrix.python-version }} ${{ matrix.resolution }}
    runs-on: ${{ matrix.platform }}
    env:
      # disable resyncing on uv run
      UV_NO_SYNC: "1"
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13"]
        resolution: [highest, lowest-direct]

    steps:
      - uses: actions/checkout@v6
      # The Ubuntu runner image ships broken Microsoft apt repos whose signature
      # is periodically invalid ("Clearsigned file isn't valid, got 'NOSPLIT'"),
      # which fails the apt-get update inside setup-qt-libs. We don't need them.
      - name: Remove broken Microsoft apt sources (ubuntu runner image)
        if: runner.os == 'Linux'
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list \
                     /etc/apt/sources.list.d/azure-cli.list \
                     /etc/apt/sources.list.d/azure-cli.sources
      - uses: tlambert03/setup-qt-libs@v1

      # The tree view renders via fastplotlib/pygfx/wgpu, which needs a graphics
      # adapter. Headless Linux runners have no GPU, so install Mesa's software
      # Vulkan driver (lavapipe); wgpu then finds an adapter under xvfb instead of
      # aborting (SIGABRT in request_adapter). macOS (Metal) / Windows (DX12) are fine.
      - name: Install software Vulkan (lavapipe) for headless wgpu
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers libvulkan1 vulkan-tools libgl1-mesa-dri
          # Point the Vulkan loader straight at lavapipe (the software ICD) and force
          # software GL, so wgpu's request_adapter finds an adapter instead of aborting.
          # $GITHUB_ENV persists these to the later test step.
          echo "VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json" >> "$GITHUB_ENV"
          echo "LIBGL_ALWAYS_SOFTWARE=1" >> "$GITHUB_ENV"
          # Diagnostic: show what the loader sees (never fails the job).
          vulkaninfo --summary || true

      - name: Set up Python ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Install dependencies
        run: uv sync --no-dev --group testing --resolution ${{ matrix.resolution }}

      - name: Set Windows resolution
        if: runner.os == 'Windows'
        run: Set-DisplayResolution -Width 1920 -Height 1080 -Force
        shell: powershell

      - name: Install Windows OpenGL
        if: runner.os == 'Windows'
        run: |
          git clone --depth 1 https://github.com/pyvista/gl-ci-helpers.git
          powershell gl-ci-helpers/appveyor/install_opengl.ps1
          if (Test-Path -Path "C:\Windows\system32\opengl32.dll" -PathType Leaf) {Exit 0} else {Exit 1}
        shell: powershell

      # Diagnostic: vulkaninfo shows lavapipe is present, yet wgpu aborts in
      # request_adapter without surfacing the Rust error. Request an adapter
      # directly with Rust logging on so the real failure reason appears in the log.
      - name: Diagnose wgpu adapter (Linux)
        if: runner.os == 'Linux'
        continue-on-error: true
        uses: aganders3/headless-gui@v2
        env:
          RUST_BACKTRACE: "full"
          RUST_LOG: "wgpu_core=info,wgpu_hal=info,naga=warn"
        with:
          run: uv run python -c "import wgpu; print('ADAPTERS:', [a.summary for a in wgpu.backends.wgpu_native.enumerate_adapters()])"

      - name: Test
        uses: aganders3/headless-gui@v2
        with:
          run: uv run pytest --color=yes --cov=motile_tracker --cov-report=xml --cov-report=term-missing .

      - name: Coverage
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: funkelab/motile_tracker
