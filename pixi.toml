[project]
name = "motile-tracker"
channels = [
  "conda-forge",
  "funkelab",
  "gurobi",
]

platforms = ["win-64", "linux-64", "osx-arm64", "osx-64"]
preview = [ "pixi-build" ]

[dependencies]
python = "3.11.*"
pip = "*"
gcg = "*"
papilo = "*"
scip = "*"
soplex = "*"
zimpl = "*"
ilpy = "0.4.0"

[tasks.setup]
cmd = "pip install ."

[tasks.create-package]
depends-on = [ "setup" ]
cmd = "pixi exec constructor . --output-dir dist/"

[tasks.start]
depends-on = [ "setup" ]
cmd = "python -m motile_tracker.launcher"

[package]
name = "motile-tracker"
version = "v3.1.3"

[package.build]
backend = { name = "pixi-build-python", version = "0.1.*" }

[package.host-dependencies]
constructor = "*"
